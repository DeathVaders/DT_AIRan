%% Compatibility entry for Distance + DT stacked error generation
% The stacked workflow lives in distance_main.m so both old entry points
% generate the same BS%d_UE%d_locDT.mat result files.
run("distance_main.m");
